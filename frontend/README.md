# DockB Frontend

React + Tiptap editor for the DockB document hierarchy.

## Tech Stack

- **React 18** — UI framework
- **Tiptap 2** — ProseMirror-based rich text editor
- **Vite** — Build tool with dev server proxy to backend
- **TypeScript** — Type safety matching backend wire format

## Architecture

The frontend communicates with the FastAPI backend via REST endpoints defined in
`src/dockb/controllers/README_API.md`. All document content uses ProseMirror JSON
wire format (chapter → paragraph → sentence → text).

### Document Hierarchy

```
Document (plain JSON)
  └── Chapter (ProseMirror tree)
        └── Paragraph (ProseMirror tree)
              └── Sentence (ProseMirror tree)
                    └── Text (inline)
```

### Wire Format Mapping

| Backend Node   | Tiptap Extension   | Top Node |
|----------------|---------------------|----------|
| Chapter        | `Chapter`           | Yes      |
| Paragraph      | `Paragraph`         | No       |
| Sentence       | `Sentence`          | No       |
| Text           | Built-in            | No       |

### Key Design Decisions

- **Tokens are server-only** — spaCy tokenization happens in the backend; the
  frontend only sees text nodes
- **Semantic events, not text diffs** — the frontend sends whole sentence/paragraph
  replacements; the backend handles sentence splitting asynchronously
- **IDs are UUIDs v4** — generated by creator (frontend or backend), no temp-ID mapping
- **Notifications** — sentence/paragraph splits arrive via piggy-back on mutation
  responses or polling `GET /api/notifications`
- **Transaction-based saves** — the editor listens to ProseMirror transactions
  via Tiptap's `onTransaction` event. `getChangedRanges()` identifies which
  sentence nodes were touched, and only those sentences are PUT to the backend.

## Structure

```
frontend/
├── src/
│   ├── api/client.ts          # API client for all REST endpoints
│   ├── editor/
│   │   ├── schema.ts          # ProseMirror schema matching backend
│   │   ├── ChapterEditor.tsx  # Tiptap editor for chapter content
│   │   ├── extensions.ts      # Custom Tiptap node extensions
│   │   └── extractChanges.ts  # Transaction diff → sentence mutations
│   ├── types/index.ts         # TypeScript types matching wire format
│   ├── components/
│   │   ├── DocumentList.tsx   # Document browser + create button
│   │   └── ChapterView.tsx    # Chapter sidebar + editor
│   ├── App.tsx                # Main app with navigation
│   └── main.tsx               # Entry point
├── tests/
│   ├── components/
│   │   ├── DocumentList.test.tsx
│   │   └── ChapterView.test.tsx
│   └── editor/
│       └── extractChanges.test.ts
├── package.json
├── vite.config.ts             # Dev server + API proxy
└── .eslintrc.cjs
```

### Creating Documents

The Document List screen shows a "New Document" button. Clicking it opens an
inline form with **Title** and **Author** fields. On submit the frontend:

1. Generates a UUID v4 for the document ID
2. POSTs to `POST /api/documents` with `{ attrs: { id, title, author } }`
3. Refreshes the document list to show the new document

### Semantic Event Flow

When the user types in the editor, Tiptap fires a ProseMirror transaction.
`extractChanges()` uses `combineTransactionSteps()` and `getChangedRanges()`
to identify which sentence nodes were touched. For each affected sentence it
emits a minimal mutation:

- **update** — sentence text changed (typing, backspace within a sentence)
- **create** — new sentence appeared (Enter split within a sentence)
- **delete** — sentence removed (period deleted, sentences merged)

These mutations are sent individually via `api.sentences.update/create/delete`.
The backend handles tokenization and sentence splitting asynchronously.

## Development

```bash
cd frontend
npm install
npm run dev    # Serves on :3000, proxies /api to :8000
```

The Vite dev server proxies `/api` requests to `http://localhost:8000` where the
FastAPI backend runs.

## Commands

| Command         | Purpose                          |
|-----------------|----------------------------------|
| `npm run dev`   | Start dev server                 |
| `npm run build` | TypeScript check + production build |
| `npm run lint`  | ESLint check                     |
| `npm test`      | Run unit tests (vitest)          |

## Testing

Tests use **vitest** with **@testing-library/react** and **jsdom**. Test files
live alongside or near the components they cover (e.g. `tests/components/`).

```bash
npm test            # run once
npm test -- --watch # watch mode
```
