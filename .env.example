GITHUB_API_KEY=
OPENAI_API_KEY=
NEO4J_URL=bolt://localhost:7687
NEO4J_PASSWORD=
NEO4J_USER=
NEO4J_DATABASE=

# Local (non-OAuth) mode: with these set (and no OAuth provider client
# id+secret pair configured), the backend identifies the user as the OS
# username and skips OAuth sign-in (README_auth.md section 6).
DOCKB_CHAPTERS_DIR=
DOCKB_SECRET_KEY=

